module.exports = {
  project: {
    // Укажи, где находится твой проект (например, в папке "dev")
    root: "./dev",
    // Если проект на JavaScript, можешь указать jsconfig.json, иначе tsconfig.json
    tsConfig: "./dev/jsconfig.json"
  },
  framework: "vue"
}
